name: Build WinLocker C++

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-winlocker:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup MSVC Developer Command Prompt
      uses: ilammy/msvc-dev-cmd@v1

    - name: Build EXE
      shell: cmd
      # Фикс: добавлена advapi32.lib для работы с реестром
      run: |
        cl /EHsc main.cpp /link user32.lib gdi32.lib comctl32.lib advapi32.lib /OUT:winlocker_prank.exe
        if %errorlevel% neq 0 exit /b %errorlevel%

    - name: Upload EXE artifact
      uses: actions/upload-artifact@v4
      with:
        name: winlocker-exe
        path: winlocker_prank.exe
        retention-days: 30
